\echo Use "ALTER EXTENSION fasttrun UPDATE TO '2.5.0'" to load this file. \quit

-- Учет износа статистики по колонкам реализован в C-библиотеке.
-- SQL-объекты не меняются.
--
-- ВНИМАНИЕ: учет по колонкам работает только там, где библиотека предзагружена.
-- Добавьте fasttrun в shared_preload_libraries или session_preload_libraries.
-- Вторую настройку загружают только клиентские сеансы, фоновым процессам нужна
-- первая. Без предзагрузки износ считается по таблице целиком, и клиентский
-- сеанс один раз пишет об этом в серверный лог.
