\echo Use "ALTER EXTENSION fasttrun UPDATE TO '2.5.0'" to load this file. \quit

-- Учёт износа статистики по колонкам реализован в C-библиотеке.
-- SQL-объекты не меняются.
--
-- ВНИМАНИЕ: учёт по колонкам работает только там, где библиотека предзагружена.
-- Добавьте fasttrun в session_preload_libraries, иначе поведение останется
-- прежним, по таблице целиком, и об этом будет одна строка в серверном логе.
